# This configuration file tells GraphQL Code Generator how
# to generate types based on our schema.
documents:
  - "./src/**/*.ts"
schema: "../../graphql-service/apollo/__generated__/Public.final.graphql"
generates:
  # Specify where our generated types should live.
  ./src/__generated__/gql/:
    preset: client
    config:
      typesPrefix: GQL
      scalars:
        Timestamp: number
        URL: string
        Email: string
        Any: any
