{
  "name": "cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "poetry build",
    "deploy": "npm run build && poetry publish -r testpypi",
    "typecheck": "pyright .",
    "gen": "turms gen",
    "watch": "nodemon --exec 'npm run gen && npm run typecheck' --watch . --ext py,json,graphql,yaml --delay 1 --ignore '**/__pycache__/' --ignore '**/__generated__/**'"
  },
  "author": "",
  "license": "ISC"
}
