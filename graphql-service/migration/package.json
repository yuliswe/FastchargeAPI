{
  "name": "dynamodb-migration-tool",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle --outdir=./dist/ --platform=node --format=cjs --out-extension:.js=.cjs ./src/index.ts",
    "cli": "npm run build && node ./dist/index.cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^10.0.0"
  }
}
